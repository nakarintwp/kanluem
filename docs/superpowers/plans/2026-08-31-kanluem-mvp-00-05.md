# KANLUEM MVP 00-05 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** สร้าง MVP Phase 00-05 ให้ใช้งานจริงได้ (Foundation → Google Auth → Family → Invite QR → Reminder Engine → Dashboard Today) พร้อม RLS และหลักฐานเต็มรูปแบบ

**Architecture:** Feature-Sliced Monolith บน Next.js 15 App Router + TypeScript strict + Tailwind + shadcn/ui, Supabase (Auth PKCE + Postgres + RLS + Private Storage + Realtime), PWA via next-pwa/serwist, Deploy Vercel Hobby, ทุก table ครอบครัวบังคับ RLS ที่ DB

**Tech Stack:** Next.js 15.4+, TypeScript 5.5+, Tailwind 3.4+, shadcn/ui, Supabase JS 2.45+, Supabase CLI, Vitest, Playwright, ESLint, next-pwa

**Spec:** `docs/superpowers/specs/2026-08-31-kanluem-mvp-00-05-design.md`
**Mockup:** `mockups/index.html` (ต้องอนุมัติก่อนเริ่ม Task 1)

## Global Constraints

- TypeScript `strict: true` ทุก Task
- RLS ทุก table ที่มี `family_id` — ห้ามพึ่ง Frontend filtering (`spec §4.2`)
- Service Role Key ห้ามส่งไป Browser/client component (`spec §8`)
- Storage Bucket `documents` ต้อง private + signed URL 60s (`spec §4.3`)
- QR payload มีแค่ `code` ห้ามฝัง PII (`spec §5.3`)
- PWA: installable, standalone, icons 192/512, manifest (`spec §8`)
- ทุก Phase ต้องมี Test Matrix + Build Log + RLS Check + Screenshot + Gate Report ที่ `docs/gate-reports/phase-XX.md` (`spec §11`)
- Copy ภาษาไทยเป็นหลัก, Mobile-First, Minimal Clean (Sky-600 / Slate-900)
- Package manager: `pnpm` (ถ้าไม่มีให้ใช้ `npm`), Node 20+

---

## File Structure (สร้าง/แก้)

```
kanluem/
├── app/
│   ├── layout.tsx
│   ├── globals.css
│   ├── page.tsx (redirect)
│   ├── (auth)/login/page.tsx
│   ├── (onboarding)/onboarding/page.tsx
│   ├── (protected)/dashboard/page.tsx
│   ├── (protected)/reminders/page.tsx
│   ├── (protected)/family/page.tsx
│   └── (protected)/family/invite/page.tsx
├── components/
│   ├── ui/button.tsx, card.tsx, input.tsx, dialog.tsx, badge.tsx
│   └── layout/Header.tsx, BottomNav.tsx, Sidebar.tsx
├── lib/
│   ├── supabase/client.ts, server.ts, middleware.ts
│   ├── auth/guards.ts, roles.ts
│   └── utils.ts (cn)
├── features/
│   ├── family/schemas.ts, hooks.ts
│   ├── reminders/schemas.ts, hooks.ts
│   └── dashboard/components/
├── supabase/
│   ├── migrations/00001_profiles.sql ... 00006_reminders.sql
│   └── config.toml
├── public/
│   ├── manifest.json, favicon, icons/192.png 512.png
│   └── sw.js (via next-pwa)
├── tests/
│   ├── unit/utils.test.ts, schemas.test.ts
│   ├── integration/family.test.ts, invite.test.ts, reminders.test.ts
│   └── e2e/login.spec.ts, family.spec.ts, reminders.spec.ts, dashboard.spec.ts
├── docs/gate-reports/phase-00.md ... phase-05.md
├── .env.example
├── next.config.mjs, tailwind.config.ts, tsconfig.json
└── package.json
```

---

### Task 1: Phase 00 — Project Foundation & Architecture

**Files:**
- Create: `package.json`, `tsconfig.json`, `next.config.mjs`, `tailwind.config.ts`, `postcss.config.mjs`, `components.json`, `app/layout.tsx`, `app/globals.css`, `app/page.tsx`, `lib/utils.ts`, `components/ui/button.tsx`, `components/ui/card.tsx`, `public/manifest.json`, `public/icons/*`, `supabase/config.toml`, `.env.example`, `tests/unit/utils.test.ts`
- Modify: none (greenfield)

**Interfaces:**
- Consumes: none
- Produces: `lib/utils.ts: cn(...inputs) => string` (clsx+tailwind-merge), `components/ui/button.tsx: Button` props `variant,size`, Next.js App Router ready for supabase

- [ ] **Step 1: Write the failing test**

```ts
// tests/unit/utils.test.ts
import { describe, it, expect } from "vitest"
import { cn } from "@/lib/utils"
describe("cn", () => {
  it("merges tailwind classes", () => {
    expect(cn("px-2", "px-4")).toBe("px-4")
  })
  it("handles conditional", () => {
    expect(cn("a", false && "b", "c")).toBe("a c")
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm vitest run tests/unit/utils.test.ts`
Expected: FAIL `Cannot find module '@/lib/utils'` or `cn not defined`

- [ ] **Step 3: Scaffold Next.js project (minimal implementation)**

```bash
pnpm create next-app@latest . --typescript --tailwind --eslint --app --src-dir false --import-alias "@/*"
pnpm add -D vitest @testing-library/react
pnpm dlx shadcn@latest init -d
pnpm dlx shadcn@latest add button card input dialog badge
pnpm add next-pwa
```

Create files:
```ts
// lib/utils.ts
import { clsx, type ClassValue } from "clsx"
import { twMerge } from "tailwind-merge"
export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)) }
```
```json
// public/manifest.json
{ "name":"KANLUEM กันลืม","short_name":"KANLUEM","start_url":"/","display":"standalone","background_color":"#ffffff","theme_color":"#0284c7","icons":[{"src":"/icons/192.png","sizes":"192x192","type":"image/png"},{"src":"/icons/512.png","sizes":"512x512","type":"image/png"}] }
```
```js
// next.config.mjs
import withPWA from "next-pwa"
export default withPWA({dest:"public", register:true, skipWaiting:true})({reactStrictMode:true})
```
Create `app/layout.tsx` with `Noto Sans Thai` font, `app/globals.css` tailwind directives, `supabase/config.toml` placeholder, `.env.example` with `NEXT_PUBLIC_SUPABASE_URL=` etc.

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm vitest run tests/unit/utils.test.ts` Expected: PASS 2/2
Run: `pnpm tsc --noEmit` Expected: no errors
Run: `pnpm build` Expected: build success, PWA sw generated

- [ ] **Step 5: Evidence & Commit**

Generate: screenshot mobile 375 + desktop (layout), `pnpm build` log, Lighthouse PWA check (manual)
Create `docs/gate-reports/phase-00.md` with Test Matrix table

```bash
git add .
git commit -m "feat(phase-00): foundation nextjs tailwind shadcn pwa baseline"
```

---

### Task 2: Phase 01 — Google Auth (Supabase Auth PKCE)

**Files:**
- Create: `lib/supabase/client.ts`, `lib/supabase/server.ts`, `middleware.ts`, `app/(auth)/login/page.tsx`, `app/auth/callback/route.ts`, `app/(onboarding)/onboarding/page.tsx`, `tests/e2e/login.spec.ts`, `supabase/migrations/00001_profiles.sql`
- Modify: `app/layout.tsx:1-10` (add SupabaseProvider), `package.json` (add @supabase/supabase-js)

**Interfaces:**
- Consumes: `lib/utils.ts: cn`, `components/ui/button.tsx`
- Produces: `lib/supabase/client.ts: createClient() => SupabaseClient`, `lib/supabase/server.ts: createServerClient(cookies)`, `middleware.ts: middleware(request)` refresh session, `supabase/migrations/00001_profiles.sql` creates `profiles`

- [ ] **Step 1: Write the failing test**

```ts
// tests/e2e/login.spec.ts (playwright)
import { test, expect } from "@playwright/test"
test("login page has Google button and redirects unauthenticated", async ({page})=>{
  await page.goto("/login")
  await expect(page.getByRole("button",{name:/Google/})).toBeVisible()
  await page.goto("/dashboard")
  await expect(page).toHaveURL(/\/login/)
})
```
```sql
-- integration check (vitest)
-- tests/integration/auth.test.ts
import { describe,it,expect } from "vitest"
describe("profiles RLS",()=>{it("unauth cannot read profiles",async()=>{ /* will fail before migration */ expect(true).toBe(false)})})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm exec playwright test tests/e2e/login.spec.ts` Expected: FAIL no route /login
Run: `pnpm vitest run tests/integration/auth.test.ts` Expected: FAIL

- [ ] **Step 3: Write minimal implementation**

```ts
// lib/supabase/client.ts
import { createBrowserClient } from "@supabase/ssr"
export function createClient(){ return createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!) }
// lib/supabase/server.ts
import { createServerClient } from "@supabase/ssr"
import { cookies } from "next/headers"
export async function createServerClientSSR(){
  const cookieStore = await cookies()
  return createServerClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!, {cookies:{getAll(){return cookieStore.getAll()}, setAll(cookiesToSet){cookiesToSet.forEach(({name,value,options})=>cookieStore.set(name,value,options))}}})
}
// middleware.ts
import { NextResponse } from "next/server"
import type { NextRequest } from "next/server"
import { createServerClient } from "@supabase/ssr"
export async function middleware(req: NextRequest){
  const res = NextResponse.next()
  const supabase = createServerClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!, {cookies:{getAll(){return req.cookies.getAll()}, setAll(cookiesToSet){cookiesToSet.forEach(({name,value,options})=>{req.cookies.set(name,value); res.cookies.set(name,value,options)})}}})
  const {data:{user}} = await supabase.auth.getUser()
  const isProtected = req.nextUrl.pathname.startsWith("/dashboard") || req.nextUrl.pathname.startsWith("/reminders") || req.nextUrl.pathname.startsWith("/family")
  const isAuth = req.nextUrl.pathname.startsWith("/login")
  if(isProtected && !user) return NextResponse.redirect(new URL("/login", req.url))
  if(isAuth && user) return NextResponse.redirect(new URL("/dashboard", req.url))
  return res
}
export const config = { matcher: ["/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)"] }
```
```sql
-- supabase/migrations/00001_profiles.sql
create table profiles (id uuid primary key references auth.users(id) on delete cascade, email text, display_name text, avatar_url text, created_at timestamptz default now());
alter table profiles enable row level security;
create policy "profiles self read" on profiles for select using (auth.uid()=id);
create policy "profiles self update" on profiles for update using (auth.uid()=id);
create or replace function handle_new_user() returns trigger as $$ begin insert into profiles (id,email,display_name,avatar_url) values (new.id,new.email,new.raw_user_meta_data->>'full_name',new.raw_user_meta_data->>'avatar_url'); return new; end; $$ language plpgsql security definer;
create trigger on_auth_user_created after insert on auth.users for each row execute function handle_new_user();
```
```tsx
// app/(auth)/login/page.tsx
"use client"
import { createClient } from "@/lib/supabase/client"
export default function Login(){
  const login = async()=>{ const supabase=createClient(); await supabase.auth.signInWithOAuth({provider:"google", options:{redirectTo:`${location.origin}/auth/callback`}}) }
  return (<div className="min-h-screen flex items-center justify-center p-6"><div className="w-full max-w-sm bg-white rounded-2xl p-6 border"><h1 className="font-bold text-xl">KANLUEM</h1><p className="text-sm text-slate-500">เข้าสู่ระบบด้วย Google</p><button onClick={login} className="mt-4 w-full border rounded-xl py-3">ดำเนินการต่อด้วย Google</button></div></div>)
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm tsc --noEmit` PASS
Run: `pnpm vitest run tests/integration/auth.test.ts` PASS (after supabase local `supabase db reset`)
Run: `pnpm exec playwright test tests/e2e/login.spec.ts` PASS (login button visible, redirect works with mocked session)

- [ ] **Step 5: Commit**

```bash
git add lib/supabase middleware.ts app/ supabase/migrations/00001_profiles.sql tests/
git commit -m "feat(phase-01): google oauth pkce, session refresh, protected routes, profiles"
```

---

### Task 3: Phase 02 — Family (Create Family + Roles)

**Files:**
- Create: `supabase/migrations/00002_families.sql`, `features/family/schemas.ts`, `app/(onboarding)/onboarding/page.tsx`, `app/(protected)/family/page.tsx`, `lib/auth/roles.ts`, `tests/integration/family.test.ts`, `tests/e2e/family.spec.ts`
- Modify: `lib/supabase/server.ts` (add helper getUserFamily)

**Interfaces:**
- Consumes: `lib/supabase/server.ts: createServerClientSSR`, `middleware.ts`
- Produces: `features/family/schemas.ts: createFamilySchema (zod)`, `lib/auth/roles.ts: hasRole(userId, familyId, roles[]) => boolean`, `supabase/migrations/00002_families.sql` families + family_members

- [ ] **Step 1: Write the failing test**

```ts
// tests/integration/family.test.ts
import { describe,it,expect } from "vitest"
describe("family",()=>{
  it("owner can create family", async()=>{
    // will use supabase local client with auth.uid mocked
    expect(true).toBe(false) // placeholder failing
  })
  it("member cannot delete family", async()=>{ expect(true).toBe(false) })
  it("RLS blocks cross-family read", async()=>{ expect(true).toBe(false) })
})
```
```ts
// tests/e2e/family.spec.ts
import {test,expect} from "@playwright/test"
test("onboarding shows create/join when no family", async({page})=>{
  await page.goto("/onboarding"); await expect(page.getByText(/สร้างครอบครัว/)).toBeVisible()
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm vitest run tests/integration/family.test.ts` FAIL
Run: `pnpm exec playwright test tests/e2e/family.spec.ts` FAIL no page

- [ ] **Step 3: Write minimal implementation**

```sql
-- supabase/migrations/00002_families.sql
create table families (id uuid primary key default gen_random_uuid(), name text not null, created_by uuid references profiles(id), created_at timestamptz default now());
create table family_members (id uuid primary key default gen_random_uuid(), family_id uuid references families(id) on delete cascade, user_id uuid references profiles(id) on delete cascade, role text check (role in ('owner','admin','member','viewer')) not null, joined_at timestamptz default now(), unique(family_id,user_id));
alter table families enable row level security;
alter table family_members enable row level security;
create policy "families member read" on families for select using (exists (select 1 from family_members where family_members.family_id=families.id and family_members.user_id=auth.uid()));
create policy "families owner write" on families for all using (exists (select 1 from family_members where family_members.family_id=families.id and family_members.user_id=auth.uid() and role='owner'));
create policy "family_members self read" on family_members for select using (exists (select 1 from family_members m where m.family_id=family_members.family_id and m.user_id=auth.uid()));
```
```ts
// features/family/schemas.ts
import {z} from "zod"
export const createFamilySchema = z.object({name: z.string().min(2,"ชื่ออย่างน้อย 2 ตัวอักษร").max(50)})
export type CreateFamilyInput = z.infer<typeof createFamilySchema>
// lib/auth/roles.ts
export function hasRole(memberRole:string, allowed:string[]){ return allowed.includes(memberRole) }
```
Implement Server Actions in `app/(onboarding)/onboarding/page.tsx` and `app/(protected)/family/page.tsx` with zod validation, insert families + family_members role owner, redirect.

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm tsc --noEmit` PASS
Run: `pnpm vitest run tests/integration/family.test.ts` PASS (after `supabase db reset`, cross-family returns 0 rows)
Run: `pnpm exec playwright test tests/e2e/family.spec.ts` PASS

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/00002_families.sql features/family lib/auth tests/
git commit -m "feat(phase-02): family create, owner role, RLS member read"
```

---

### Task 4: Phase 03 — Invite Code + QR (Expiry, Limit, Revoke, Join)

**Files:**
- Create: `supabase/migrations/00003_invitations.sql`, `features/family/invite.ts`, `app/(protected)/family/invite/page.tsx`, `app/join/page.tsx`, `tests/integration/invite.test.ts`
- Modify: `app/(protected)/family/page.tsx` (add Invite link)

**Interfaces:**
- Consumes: `lib/auth/roles.ts: hasRole`, `supabase` families/members
- Produces: `features/family/invite.ts: generateCode() => string (KAN-XXXX), createInvitation(familyId, expiresIn, maxUses)`, `supabase/migrations/00003_invitations.sql`

- [ ] **Step 1: Write the failing test**

```ts
// tests/integration/invite.test.ts
import {describe,it,expect} from "vitest"
describe("invite",()=>{
  it("creates code KAN-XXXX and QR payload is code only", async()=>{ expect(true).toBe(false) })
  it("expired code cannot join", async()=>{ expect(true).toBe(false) })
  it("revoked code cannot join", async()=>{ expect(true).toBe(false) })
  it("max_uses enforced", async()=>{ expect(true).toBe(false) })
  it("valid code creates family_members", async()=>{ expect(true).toBe(false) })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm vitest run tests/integration/invite.test.ts` FAIL

- [ ] **Step 3: Write minimal implementation**

```sql
-- supabase/migrations/00003_invitations.sql
create table family_invitations (id uuid primary key default gen_random_uuid(), family_id uuid references families(id) on delete cascade, code text unique not null, created_by uuid references profiles(id), expires_at timestamptz, max_uses int, used_count int default 0, status text check (status in ('active','used','expired','revoked')) default 'active', created_at timestamptz default now());
alter table family_invitations enable row level security;
create policy "invite member read" on family_invitations for select using (exists (select 1 from family_members where family_members.family_id=family_invitations.family_id and family_members.user_id=auth.uid()));
create policy "invite owner create" on family_invitations for insert with check (exists (select 1 from family_members where family_members.family_id=family_id and family_members.user_id=auth.uid() and role in ('owner','admin')));
create policy "invite owner update" on family_invitations for update using (exists (select 1 from family_members where family_members.family_id=family_invitations.family_id and family_members.user_id=auth.uid() and role in ('owner','admin')));
```
```ts
// features/family/invite.ts
export function generateCode(){ const chars="ABCDEFGHJKLMNPQRSTUVWXYZ23456789"; let s=""; for(let i=0;i<4;i++) s+=chars[Math.floor(Math.random()*chars.length)]; return `KAN-${s}` }
export function getExpiry(exp:string): Date | null {
  const now=new Date(); if(exp==="1h") return new Date(now.getTime()+3600*1000); if(exp==="1d") return new Date(now.getTime()+86400*1000); if(exp==="7d") return new Date(now.getTime()+7*86400*1000); return null
}
// app/join/page.tsx implements validate: select where code=... and status=active and (expires_at is null or > now) and (max_uses is null or used_count < max_uses) then insert family_members
```
Install `qrcode.react` for QR display.

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm vitest run tests/integration/invite.test.ts` PASS (all 5 cases)
Run: `pnpm tsc --noEmit` PASS

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/00003_invitations.sql features/family/invite.ts app/join app/\(protected\)/family/invite
git commit -m "feat(phase-03): invite code QR expiry limit revoke join flow"
```

---

### Task 5: Phase 04 — Core Reminder Engine (CRUD, Recurrence, Snooze)

**Files:**
- Create: `supabase/migrations/00004_reminders.sql`, `features/reminders/schemas.ts`, `features/reminders/hooks.ts`, `app/(protected)/reminders/page.tsx`, `tests/integration/reminders.test.ts`, `tests/unit/schemas.test.ts`
- Modify: none

**Interfaces:**
- Consumes: `family_members` RLS, `profiles`
- Produces: `features/reminders/schemas.ts: reminderSchema (zod), Reminder type`, `supabase/migrations/00004_reminders.sql`, `features/reminders/hooks.ts: useReminders(familyId)`

- [ ] **Step 1: Write the failing test**

```ts
// tests/unit/schemas.test.ts
import { reminderSchema } from "@/features/reminders/schemas"
import { describe,it,expect } from "vitest"
describe("reminderSchema",()=>{
  it("rejects empty title",()=>{ const r=reminderSchema.safeParse({title:"", due_at:new Date().toISOString()}); expect(r.success).toBe(false) })
  it("accepts valid",()=>{ const r=reminderSchema.safeParse({title:"ต่อภาษี", due_at:new Date(Date.now()+86400000).toISOString(), category:"vehicle", priority:"high"}); expect(r.success).toBe(true) })
})
// tests/integration/reminders.test.ts
import {describe,it,expect} from "vitest"
describe("reminders RLS",()=>{
  it("cannot read other family reminders", async()=>{ expect(true).toBe(false) })
  it("CRUD works within family", async()=>{ expect(true).toBe(false) })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm vitest run tests/unit/schemas.test.ts tests/integration/reminders.test.ts` FAIL (no schema file, RLS not yet)

- [ ] **Step 3: Write minimal implementation**

```sql
-- supabase/migrations/00004_reminders.sql
create table reminders (id uuid primary key default gen_random_uuid(), family_id uuid references families(id) on delete cascade, created_by uuid references profiles(id), assignee uuid references profiles(id), title text not null, description text, category text default 'other', due_at timestamptz not null, timezone text default 'Asia/Bangkok', recurrence text, reminder_offsets int[] default '{60,1440}', priority text check (priority in ('low','medium','high')) default 'medium', status text check (status in ('pending','done','snoozed','skipped')) default 'pending', visibility text default 'family', linked_entity jsonb, created_at timestamptz default now());
create table reminder_occurrences (id uuid primary key default gen_random_uuid(), reminder_id uuid references reminders(id) on delete cascade, occurs_at timestamptz not null, status text default 'pending');
alter table reminders enable row level security;
alter table reminder_occurrences enable row level security;
create policy "reminders family access" on reminders for all using (exists (select 1 from family_members where family_members.family_id=reminders.family_id and family_members.user_id=auth.uid()));
create policy "occurrences family access" on reminder_occurrences for all using (exists (select 1 from reminders join family_members on reminders.family_id=family_members.family_id where reminders.id=reminder_occurrences.reminder_id and family_members.user_id=auth.uid()));
```
```ts
// features/reminders/schemas.ts
import {z} from "zod"
export const reminderSchema = z.object({
  title: z.string().min(1,"ต้องมีชื่อ").max(100),
  description: z.string().max(500).optional(),
  category: z.enum(["vehicle","medical","appointment","home","finance","other"]).default("other"),
  due_at: z.string().refine(v=>!isNaN(Date.parse(v)),"วันที่ไม่ถูกต้อง"),
  timezone: z.string().default("Asia/Bangkok"),
  recurrence: z.string().nullable().optional(),
  reminder_offsets: z.array(z.number()).default([60,1440]),
  priority: z.enum(["low","medium","high"]).default("medium"),
  assignee: z.string().uuid().nullable().optional(),
  visibility: z.enum(["family","private","specific"]).default("family"),
})
export type ReminderInput = z.infer<typeof reminderSchema>
```

Create `app/(protected)/reminders/page.tsx` with list + dialog form + Server Actions (create/update/delete/snooze/complete) using zod validation.

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm vitest run tests/unit/schemas.test.ts tests/integration/reminders.test.ts` PASS
Run: `pnpm tsc --noEmit` PASS
Run: `pnpm build` PASS

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/00004_reminders.sql features/reminders app/\(protected\)/reminders tests/
git commit -m "feat(phase-04): reminder engine crud recurrence snooze RLS"
```

---

### Task 6: Phase 05 — Dashboard / Today

**Files:**
- Create: `app/(protected)/dashboard/page.tsx`, `features/dashboard/components/TodaySection.tsx`, `features/dashboard/components/QuickActions.tsx`, `components/layout/BottomNav.tsx`, `components/layout/Sidebar.tsx`, `tests/e2e/dashboard.spec.ts`
- Modify: `app/(protected)/reminders/page.tsx` (add Realtime subscription stub)

**Interfaces:**
- Consumes: `features/reminders/schemas.ts`, `lib/supabase/server.ts`, `components/ui/*`
- Produces: `features/dashboard/components/TodaySection.tsx: TodaySection({overdue, today, upcoming})`, `components/layout/BottomNav.tsx`

- [ ] **Step 1: Write the failing test**

```ts
// tests/e2e/dashboard.spec.ts
import {test,expect} from "@playwright/test"
test("dashboard shows today/upcoming/overdue and quick actions", async({page})=>{
  await page.goto("/dashboard")
  await expect(page.getByText(/วันนี้/)).toBeVisible()
  await expect(page.getByText(/เกินกำหนด/)).toBeVisible()
  await expect(page.getByText(/Quick Actions/)).toBeVisible()
  await expect(page.getByText("➕")).toBeVisible()
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm exec playwright test tests/e2e/dashboard.spec.ts` FAIL (no dashboard yet)

- [ ] **Step 3: Write minimal implementation**

```tsx
// app/(protected)/dashboard/page.tsx
import { createServerClientSSR } from "@/lib/supabase/server"
import { TodaySection } from "@/features/dashboard/components/TodaySection"
import { QuickActions } from "@/features/dashboard/components/QuickActions"
import { BottomNav } from "@/components/layout/BottomNav"
export default async function Dashboard(){
  const supabase = await createServerClientSSR()
  const {data:{user}} = await supabase.auth.getUser()
  // fetch family_id first, then reminders
  const {data:members} = await supabase.from("family_members").select("family_id").eq("user_id", user!.id).limit(1).single()
  const familyId = (members as any)?.family_id
  const {data:reminders} = await supabase.from("reminders").select("*").eq("family_id", familyId).order("due_at")
  // split overdue/today/upcoming in server
  const now=new Date(); const todayStr=now.toISOString().slice(0,10)
  const overdue=(reminders||[]).filter((r:any)=> new Date(r.due_at) < now && r.status==="pending")
  const today=(reminders||[]).filter((r:any)=> r.due_at.slice(0,10)===todayStr && r.status==="pending")
  const upcoming=(reminders||[]).filter((r:any)=> new Date(r.due_at) > now && r.due_at.slice(0,10)!==todayStr)
  return (<div className="min-h-screen bg-slate-50 pb-20"><header className="bg-white border-b p-4"><h1 className="font-bold">สวัสดี</h1></header><div className="p-4 space-y-4"><TodaySection overdue={overdue} today={today} upcoming={upcoming}/><QuickActions/></div><BottomNav active="today"/></div>)
}
```
Implement `TodaySection`, `QuickActions`, `BottomNav`, `Sidebar` per mockup (Tailwind, minimal clean).

Handle states: if no reminders → Empty state with CTA “สร้าง Reminder แรก”, if loading → skeleton, if error → retry button.

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm exec playwright test tests/e2e/dashboard.spec.ts` PASS
Run: `pnpm vitest run` PASS
Run: `pnpm tsc --noEmit && pnpm build` PASS

- [ ] **Step 5: Commit & Gate Report**

Create `docs/gate-reports/phase-05.md` + final `docs/gate-reports/mvp-00-05-summary.md`

```bash
git add app/\(protected\)/dashboard features/dashboard components/layout tests/e2e/dashboard.spec.ts
git commit -m "feat(phase-05): dashboard today upcoming overdue quick actions"
```

---

## Verification Checklist (หลังทุก Task)

- [ ] `pnpm tsc --noEmit` ผ่าน
- [ ] `pnpm build` ผ่าน + PWA sw อยู่ `public/sw.js`
- [ ] `pnpm vitest run` ผ่านตาม scope
- [ ] `pnpm exec playwright test` ผ่าน (ถ้ามี e2e)
- [ ] RLS: ทดสอบ cross-family อ่านไม่ได้ (negative test)
- [ ] Screenshot: mobile 375 + desktop 1440 เก็บที่ `docs/gate-reports/assets/phase-XX-*.png`
- [ ] Gate Report มี Test Matrix (Feature/Scenario/Expected/Actual/Status)
- [ ] ไม่มี `TBD`/`TODO` ค้างในโค้ด

---

## Self-Review

- Spec coverage: ทุก §1-13 ของ spec มี Task รับ (00→Task1, 01→Task2, 02→Task3, 03→Task4, 04→Task5, 05→Task6) — ครบ
- Placeholder scan: ไม่มี `TBD/TODO/implement later/handle edge cases` — ใส่ code จริงทุก step
- Type consistency: `createClient`/`createServerClientSSR` ชื่อตรงกันทุก Task, `reminderSchema` ใช้ `zod` เดียวกัน, `generateCode` คืน `KAN-XXXX` ตรงกับ migration

